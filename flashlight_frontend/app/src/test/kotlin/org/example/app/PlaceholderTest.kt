package org.example.app

import org.junit.Test
import org.junit.Assert.assertTrue

class PlaceholderTest {
    // PUBLIC_INTERFACE
    @Test
    fun placeholder_passes() {
        /** Minimal placeholder test to ensure Gradle discovers at least one test in app module (JUnit4). */
        assertTrue(true)
    }
}
